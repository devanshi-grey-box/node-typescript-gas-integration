name: Test GitHub Secret

on:
  workflow_dispatch:   # allows you to manually trigger

jobs:
  test-secret:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Print secret (GitHub will mask it)
        run: |
          echo "Using secret: ${{ secrets.TEST_SECRET }}"
